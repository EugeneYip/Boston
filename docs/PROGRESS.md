# BOSTON — build log

Append one entry per critic iteration. Newest at the bottom.

| iter | date | overall | biggest failures | actions taken |
|------|------|---------|------------------|---------------|
| 0 | 2026-08-26 | n/a | Foundation only: engine, HDR post stack, Rapier, capture harness. Placeholder city is a grid of boxes. | Launched wave 1: materials, city/roads, atmosphere, lighting, vehicles, render pipeline. |
| 1 | 2026-08-27 | ~2/10 | Session limit killed all 10 agents mid-work, but 32k lines survived and all 70 files parse. Boot crashed on a `const` decrement in Vegetation (fixed). **City.js was never written** — the orchestrator that publishes `city.plots`/`roads`/`groundHeight`, so Buildings falls back to synthesized parcels and the skyline is untextured boxes. Landmarks.js missing entirely. **5 fps at street level** (636 draws, 3.81M tris) vs 60 fps budget; 24 fps on skyline. Crushed black shadows, blown-out ground, flat cloudless sky, no aerial perspective. | Resumed 6 cut-off agents with targeted priorities (city→City.js keystone, buildings→Landmarks.js + LOD facades, lighting→crushed blacks, atmosphere→missing clouds, materials→albedo calibration, render→per-stage ms audit). Launched a dedicated profiler agent to produce an evidence-backed cost breakdown. Cleaned City-composed helpers out of the loader list. |
| 2 | 2026-08-27 | blocked | **City keystone landed**: City.js orchestrator publishes the full contract — 504 edges / 375 nodes / 96 km of street, 11,219 parcels, 1,513 spawns. Buildings jumped to 6,942 real buildings; Landmarks.js written (1,237 lines, 20 landmarks incl. 200 Clarendon, Pru, State House dome, Custom House, Fenway/Green Monster, Citgo, Zakim). Props 33,770 instances, vegetation 19,405 plants, 1,500 street lamps, 3 cascades. **BLOCKER: composer renders pure black** — direct scene render gives correct [70,64,53] but composer output is [0,0,0] across 12 passes; AutoExposurePass/TAAPass history-init suspected. Cold boot now ~45s (materials 12.8s, props 12.7s, city 8.7s). `road_atlas` material missing, roads on fallback. | Committed baseline (88 files, 39,096 lines). Sent render agent the black-frame evidence as priority zero; sent materials the road_atlas gap + willReadFrequently/asphalt-3.9s leads; re-baselined the profiler agent on the real workload and added boot time to its remit. |
| 3 | 2026-08-27 | ~3/10 | **Black frame FIXED** — root cause was postprocessing's `CopyPass` ignoring its `outputBuffer` arg and writing to its own target. City renders; HUD reads real street names. Lighting found a **double sRGB->linear conversion making bounce 7.8x too dark** (the crushed blacks) — fix landing. Materials init cut 12,782ms -> 3,688ms. **Perf corrected: 13 fps tab-fronted at 1080p/high, 492 draws, 2.73M tris** — the earlier 5 fps was background-throttled and a reported 83 fps did not reproduce. Crude bisect found no single dominant cost: composer-off was *slower* (6.2fps), clouds-hidden no change (contradicting an independent 66ms cloud measurement), shadows-off only +1.2fps. | Added `__boston.measureFps()` which refuses to report when `document.hidden` — two phantom perf numbers had already come from background-tab throttling — and documented the methodology in AGENT_BRIEF. Resumed all 7 agents with corrected numbers; asked atmosphere to re-verify the 66ms claim before committing to a cloud rewrite, and made the profiler's instrument the critical path. |
